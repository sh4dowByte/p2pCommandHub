services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: p2pcmd-server
    ports:
      - "${SERVER_PORT:-3000}:3000"
    environment:
      - PORT=3000
    volumes:
      - ./config.json:/app/config.json
    restart: unless-stopped

